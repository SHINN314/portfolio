---
interface Props {
	src: string;
	alt?: string;
	class?: string;
}

const { src, alt = "", class: className = "" } = Astro.props;
---

<div>
	<img src={src} alt={alt} class={className || undefined} />
</div>
